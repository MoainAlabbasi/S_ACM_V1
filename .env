# --- إعدادات الأمان ---
# هذا المفتاح السري يستخدمه جانغو لتشفير الجلسات وحماية الموقع
# (في المشاريع الحقيقية يجب أن يكون طويلاً ومعقداً، لكن هذا يكفي حالياً للتطوير)
SECRET_KEY='django-insecure-my-secret-key-12345'

# وضع التصحيح: اجعله True أثناء التطوير لرؤية الأخطاء بوضوح
# (عند النشر النهائي يجب تحويله إلى False)
DEBUG=True

# --- إعدادات قاعدة البيانات (PostgreSQL) ---
# اسم قاعدة البيانات التي أنشأتها في pgAdmin
DB_NAME='sacm_db'

# اسم المستخدم الافتراضي لقواعد بيانات بوستجرس
DB_USER='postgres'

# كلمة المرور التي قمت بتعيينها قبل قليل (تأكد أنها مطابقة لما كتبته)
DB_PASSWORD='123456'

# الاستضافة: localhost تعني أن قاعدة البيانات موجودة على نفس جهازك
DB_HOST='localhost'

# المنفذ: 5432 هو المنفذ الافتراضي لـ PostgreSQL
DB_PORT='5432'