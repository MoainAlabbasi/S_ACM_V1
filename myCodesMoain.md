# 📘 دليل التأسيس الأولي لمشروع (S-ACM)

**المسار المعتمد:** `E:\S-ACM\S_ACM_V1`

---

### 1️⃣ تهيئة بيئة العمل (Setup Environment)

لضمان عزل مكتبات المشروع، يجب الدخول إلى المجلد وإنشاء البيئة الافتراضية:

```bash
cd /d E:\S-ACM\S_ACM_V1
python -m venv venv
```

**✅ تفعيل البيئة (Windows) - خطوة ضرورية قبل أي أمر:**

```bash
.\venv\Scripts\activate
```

---

### 2️⃣ تثبيت المتطلبات (Install Dependencies)

تثبيت Django ومكتبات قاعدة البيانات وإدارة البيئة:

```bash
pip install django psycopg2-binary python-dotenv
```

**💡 لماذا هذه المكتبات؟**

- **django:** قلب المشروع (Framework).

- **psycopg2-binary:** "المترجم" الذي يربط Django بقاعدة بيانات PostgreSQL.

- **python-dotenv:** لقراءة ملف `.env` (لحماية كلمات المرور والإعدادات الحساسة).

---

### 3️⃣ إنشاء المشروع (Create Project)

لبناء ملفات Django الأساسية:

**🔴 ملاحظة حرجة: لا تنسَ النقطة (****`.`****) في نهاية الأمر!**

الفائدة: النقطة تجعل الملفات (`manage.py`) تنزل مباشرة في مجلدك الحالي دون إنشاء مجلد فرعي إضافي.

```bash
django-admin startproject sacm_project .
```

**📂 الناتج:**

- `manage.py`: أداة التحكم بالخادم والأوامر.

- `sacm_project/`: مجلد الإعدادات الرئيسية (`settings.py`, `urls.py`).

---

### 4️⃣ اختبار التشغيل (Verifying Installation)

للتأكد من سلامة التنصيب:

```bash
python manage.py runserver
```

**✅ النجاح:**

ظهور الرابط `http://127.0.0.1:8000/` يعني أن الخادم يعمل بنجاح.

---

### 5️⃣ إنشاء التطبيق الأساسي (Create App )

لفصل منطق المشروع في تطبيق مستقل:

```bash
python manage.py startapp academy
```

**💡 تذكير هام:**

بعد هذا الأمر، يجب الذهاب لملف `settings.py` وإضافة `'academy'` إلى قائمة `INSTALLED_APPS` يدوياً.

---

### 6️⃣ تثبيت المكتبات الإضافية (Extra Libs)

نحتاج مكتبة لمعالجة الصور (للملفات الشخصية) التي استخدمناها في Models:

```bash
pip install pillow
```

---

### 7️⃣ بناء قاعدة البيانات (Database Migration)

**🔴 خطوة حرجة جداً**

لتحويل كود Python (Models) إلى جداول فعلية في PostgreSQL:

**✅ تذكير: هل البيئة مفعلة؟ (****`venv`****)**

```bash
.\venv\Scripts\activate
```

1. **تجهيز ملفات الهجرة (للتطبيق الجديد):**

   ```bash
   python manage.py makemigrations academy
   ```

1. **تنفيذ الهجرة (إنشاء الجداول):**

   ```bash
   python manage.py migrate
   ```

---

### 8️⃣ إنشاء مدير النظام (Superuser)

لإنشاء حساب "الأدمن" للدخول إلى لوحة التحكم:

```bash
python manage.py createsuperuser
```

**💡 سيطلب منك:** اسم مستخدم، إيميل (اختياري)، وكلمة مرور.

---

### 9️⃣ أتمتة البيانات الأولية (Automation Magic) 🌟

بدلاً من الإدخال اليدوي الممل، نستخدم الأمر البرمجي الذي أنشأناه لملء البيانات:

1. **إنشاء هيكل المجلدات للأمر الخاص (Custom Command) في PowerShell:**

   ```
   New-Item -Path "academy/management/commands" -ItemType Directory -Force
   New-Item "academy/management/__init__.py"
   New-Item "academy/management/commands/__init__.py"
   New-Item "academy/management/commands/setup_initial_data.py"
   ```

   **⚠️ (يتم وضع كود تعبئة البيانات داخل ملف ****`setup_initial_data.py`****)**

1. **تشغيل الأمر السحري:**

   هذا الأمر يملأ الجداول بالأقسام والتخصصات والصلاحيات بضغطة زر.

   ```bash
   python manage.py setup_initial_data
   ```

---

### 🔟 التوثيق للمستقبل (Freeze Requirements)

لحفظ قائمة المكتبات لضمان عمل المشروع عند الفريق بنفس الإصدارات:

```bash
pip freeze > requirements.txt
```

**💡 ملاحظة:** يتم تنفيذ هذا الأمر في كل مرة نثبت فيها مكتبة جديدة.

---

### 1️⃣1️⃣ الرفع إلى GitHub (Push to Cloud)

لحفظ العمل ومشاركته مع الفريق:

1. **فحص الملفات المتغيرة:**

   ```bash
   
   ```

git status```

1. **إضافة كل الملفات:**

   ```bash
   
   ```

git add .```

1. **حفظ النسخة (Commit) مع رسالة توضيحية شاملة:**

   ```bash
   
   ```

git commit -m "feat: إنجاز المرحلة 1 بالكامل (Models + Admin + Auth)"```

1. **الرفع للسحابة (الفرع التطويري):**

   ```bash
   
   ```

git push -u origin develop```

---

### ✅ انتهى التأسيس!

**ملخص حالة المشروع:**

1. يعمل ببيئة معزولة (`venv`).

1. قاعدة البيانات مبنية وجاهزة (PostgreSQL).

1. فيه بيانات أولية (أقسام وتخصصات).

1. الكود محفوظ وآمن على GitHub.

