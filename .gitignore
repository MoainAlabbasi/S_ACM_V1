# ============================================================================
# 🐍 ملفات بايثون المؤقتة (Python Junk)
# ============================================================================
# هذه ملفات "مترجمة" ينشئها بايثون تلقائياً ليفهم الكود بسرعة.
# لا فائدة من رفعها لأنها تتولد تلقائياً في جهاز كل مبرمج.
__pycache__/
*.py[cod]
*$py.class
*.so

# ============================================================================
# 📦 مجلدات التثبيت والمكتبات (Distribution)
# ============================================================================
# مجلدات تنشأ عند تحويل المشروع إلى حزمة قابلة للتثبيت.
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# ============================================================================
# 🧪 ملفات الاختبار (Unit Test Coverage)
# ============================================================================
# تقارير فحص الكود والاختبارات الآلية. تهمك محلياً فقط.
htmlcov/
.tox/
.coverage
.cache
nosetests.xml
coverage.xml
*.cover
.pytest_cache/

# ============================================================================
# 🌐 البيئة الافتراضية (Virtual Environments) - هام جداً
# ============================================================================
# هذا المجلد يحتوي على آلاف الملفات للمكتبات (Django, Pillow...).
# حجمه ضخم جداً (قد يصل لـ 100 ميجا). لا نرفعه أبداً!
# بدلاً منه نرفع ملف 'requirements.txt' الصغير.
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# ============================================================================
# 🦄 إعدادات جانغو (Django Specifics)
# ============================================================================
# ملفات السجلات (Logs) والترجمة وقاعدة البيانات المحلية.
*.log
*.pot
*.pyc

# قاعدة البيانات المحلية (SQLite)
# نمنع رفعها لكي لا تمسح بيانات السيرفر ببياناتك التجريبية بالخطأ.
db.sqlite3
db.sqlite3-journal

# الملفات الثابتة المجمعة (تتولد بأمر collectstatic)
/staticfiles/
/static_root/

# 🖼️ ملفات الوسائط (Media) - هام للمشروع الأكاديمي
# هنا تُحفظ صور الدكاترة وملفات المحاضرات (PDF) التي يرفعونها.
# لا نرفعها للكود لأنها "بيانات مستخدمين" وليست "كود برمجي".
/media/
!/media/.gitkeep  # (استثناء) نرفع ملفاً فارغاً فقط لكي يتم إنشاء المجلد

# ملفات جلسات المستخدمين المؤقتة
*.session

# ============================================================================
# ⛔ المتغيرات السرية والحساسة (Security) - أخطر قسم
# ============================================================================
# يحتوي على كلمات المرور، مفاتيح API، وإعدادات السيرفر.
# رفع هذا الملف يعني أن مشروعك قد تم اختراقه!
.env
.env.local
.env.*.local
*.env
local_settings.py
settings_local.py
secrets.json
*.pem
*.key

# ============================================================================
# 🗄️ ملفات النسخ الاحتياطي لقواعد البيانات
# ============================================================================
*.sql
*.dump
*.db
*.sqlite
*.sqlite3
*.sql.gz

# ============================================================================
# 💻 إعدادات المحررات (IDEs)
# ============================================================================
# ملفات إعدادات خاصة ببرنامج VS Code أو PyCharm في جهازك أنت.
# (مثل لون الخط، الملفات المفتوحة مؤخراً...). لا تهم بقية الفريق.
# VSCode
.vscode/
*.code-workspace

# PyCharm
.idea/
*.iml
*.iws
*.ipr

# Sublime Text
*.sublime-project
*.sublime-workspace

# ============================================================================
# 🖥️ ملفات خاصة بنظام التشغيل (OS Files)
# ============================================================================
# ملفات مخفية ينشئها الويندوز أو الماك لتنظيم المجلدات (قمامة رقمية).
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~
.directory
.Trash-*

# ============================================================================
# 🗑️ ملفات مؤقتة ومضغوطة
# ============================================================================
*.bak
*.backup
*.swp
*.swo
*.tmp
logs/
tmp/
temp/
*.zip
*.tar
*.tar.gz
*.rar

# ============================================================================
# 📦 ملفات جافاسكربت (Node.js) - لو استخدمتها مستقبلاً
# ============================================================================
node_modules/
npm-debug.log*
yarn-debug.log*
package-lock.json

# ============================================================================
# ✅ استثناءات (ملفات يجب رفعها رغم القواعد السابقة)
# ============================================================================
!**/__init__.py   # ضروري لكي يعمل بايثون
!.env.example     # نرفعه لأنه مجرد مثال فارغ للفريق
!.gitkeep         # نرفعه للحفاظ على المجلدات الفارغة